{
  "name": "the-commons",
  "version": "0.0.0",
  "private": true,
  "description": "The Commons — a 2D top-down multiplayer virtual hangout town.",
  "type": "module",
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace shared && npm run build --workspace client",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "serve": "npm run build --workspace client && npm start --workspace server",
    "share": "ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=30 -R 80:localhost:2567 nokey@localhost.run",
    "lint": "npm run typecheck"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
